%h2 Post an implementation

= semantic_form_for [@post, @post.build_implementation] do |form|
  = render 'form', :f => form
  = form.buttons do
    = form.submit "Create"
    = link_to 'Back', post_path(@post)
